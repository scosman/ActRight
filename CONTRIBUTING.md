# Contributing

```sh
npm install
npm test
npm run lint
```

Tests use [vitest](https://vitest.dev/) and cover the helper scripts in `skill/scripts/`.

Formatting uses prettier:

```sh
npm run format:check   # verify
npm run format         # fix
```
